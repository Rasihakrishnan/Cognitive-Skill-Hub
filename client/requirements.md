## Packages
framer-motion | Beautiful page transitions, micro-interactions, and card animations
clsx | Utility for constructing `className` strings conditionally
tailwind-merge | Utility for safely merging Tailwind CSS classes without style conflicts
lucide-react | Premium vector icons

## Notes
Authentication is mocked via the /api/login endpoint which returns a user object.
Role-based rendering determines access to the Admin Dashboard (role === 'admin').
Leaderboard requires a calculation of accuracy based on score/totalQuestions.
